/**
 * /sessions/history — session log history page.
 *
 * Loaded client-side only (no server load) so auth state is available
 * before fetching. The page itself handles the unauthenticated state.
 */
export const ssr = false
